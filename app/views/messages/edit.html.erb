<%= form_for @message, :url => { :controller => 'messages', :action => 'update', :id => @message } do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <%= f.submit "Update" %>
<% end %>